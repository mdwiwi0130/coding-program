題目
```
 寫一個程式，輸入一個指定的整數為3，加上10，再印出其結果。

程式碼如下：
a=3;
a=a+10;

輸入範例:

輸出範例:
a變為13
```
解法
```
 /*4_2.用電腦玩加法-=不是等號(20.0)*/
#include <iostream>     
using namespace std; 
int main() { 
    int a=3; 
     a=a+10;
     cout<<"a變為"<<a;
    return 0; 
}
```
