```c
#include <stdio.h> #include <stdlib.h> #include <bits/stdc++.h> using namespace std; int cmpfunc (const void * a, const void * b){ return ( *(int*)a - *(int*)b ); } bool cmp (int a, int b){ return a<b; } int main(){ 	int t,n; 	int *ptr_t=&t,*ptr_n=&n; 	scanf("%d",ptr_t); 	for(int i=0;i<*ptr_t;i++){ 		int pi[20005]={0}; 		int *ptr_pi=pi; 		int ans=0; 		int *ptr_ans=&ans; 		if(i) printf("\n"); 		scanf("%d",ptr_n); 		for(int j=0;j<*ptr_n;j++) 			scanf("%d",&ptr_pi[j]); 		//qsort(ptr_pi, *ptr_n, sizeof(int), cmpfunc); 		sort(pi, pi+*ptr_n, cmp); 		for(int j=*ptr_n-3;j>=0;j-=3) 			*ptr_ans += ptr_pi[j]; 		printf("%d",*ptr_ans); 	} }
```
